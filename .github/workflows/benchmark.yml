name: Benchmark

on:
  pull_request:
    branches: [master]
    paths:
      # Re-run only when code that can move latency changed. Doc-only PRs stay fast.
      - 'packages/cli/src/**'
      - 'packages/lang-*/src/**'
      - 'packages/plugin-api/src/**'
      - 'packages/cli/benchmarks/**'
      - '.github/workflows/benchmark.yml'
  workflow_dispatch:
    inputs:
      size:
        description: 'Synthetic index size (symbols)'
        required: false
        default: '1000'

permissions:
  contents: read
  pull-requests: write

jobs:
  bench:
    name: MCP tool benchmarks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 10.9.0

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - name: Build workspace packages
        run: pnpm -r build

      - name: Run benchmarks
        id: bench
        env:
          BENCH_SIZE: ${{ github.event.inputs.size || '1000' }}
          BENCH_JSON: '1'
        run: |
          {
            echo "## Benchmark results (size=$BENCH_SIZE)"
            echo ""
            pnpm --filter @ctxo/cli bench
          } | tee -a $GITHUB_STEP_SUMMARY

      - name: Upload benchmark JSON
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: benchmark-results
          path: benchmark-results/latest.json
          retention-days: 30
